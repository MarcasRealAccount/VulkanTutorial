{
	"$schema": "http://json-schema.org/draft-04/schema",
	"description": "Pages schema",
	"id": "pagesSchema",
	"$defs": {
		"pageListing": {
			"description": "Page listing",
			"properties": {
				"visibleName": {
					"description": "The name shown to the user",
					"type": "string"
				},
				"sidebarId": {
					"description": "Id used to separate equally named pages in the sidebar",
					"type": "string"
				},
				"id": {
					"description": "Id used to separate equally named pages",
					"type": "string"
				},
				"subPages": {
					"description": "If present makes this page a folder like thing, meaning this page has no actual page itself but rather the sub pages inside this array might be",
					"items": {
						"$ref": "#/$defs/pageListing"
					},
					"type": "array",
					"uniqueItems": true
				}
			},
			"required": [ "visibleName", "sidebarId", "id" ],
			"type": "object"
		}
	},
	"properties": {
		"pages": {
			"description": "List of pages",
			"items": {
				"$ref": "#/$defs/pageListing"
			},
			"type": "array",
			"uniqueItems": true
		}
	},
	"required": [ "pages" ],
	"title": "Pages Schema",
	"type": "object",
	"version": "1.0.0"
}